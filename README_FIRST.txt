RESUME ANALYZER STARTER CODE

This folder is intentionally minimal. The project lead may copy these files into the new GitHub repository.
The function names and analyzer result keys are the shared team contract.

Run after dependencies are installed:
    streamlit run app.py

Do not put API keys directly in source code.
